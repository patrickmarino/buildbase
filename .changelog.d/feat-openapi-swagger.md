---
type: Added
---

- OpenAPI 3.1 documentation for the API (via utoipa): interactive Swagger UI at
  `/swagger-ui` and the raw spec at `/api-docs/openapi.json`. Every endpoint is
  documented with its request/response schemas, error envelope, and the
  `core_sid` session-cookie security scheme. In dev, browse it at
  `http://localhost:8080/swagger-ui/`.
